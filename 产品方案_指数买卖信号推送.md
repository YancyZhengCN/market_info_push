# 产品方案：指数买卖信号自动推送（akshare → Server酱个人微信）

> 文档类型：功能规格 / PRD  
> 版本：v0.1（方案评审版，不含代码实现）  
> 创建日期：2026-08-22  
> 状态：待评审

---

## 1. 背景与动机（Why）

用户每日需要手动查看多个宽基/主题指数的 MACD 走势，以判断当天哪些指数"适合买入"。该过程存在三个痛点：

1. **重复性高**：每天收盘后需逐个打开行情软件、切不同周期（日/2日/周）看 MACD，耗时且易漏看。
2. **口径不统一**：日线、2日线、周线 MACD 需要分别人工切换计算，主观性强。
3. **信息滞后与分散**：判断结果只在个人脑中，无法沉淀成可复盘、可分享的团队信号。

**核心假设（已与用户对齐）**：用户已有一套成熟的 MACD 选指方法论（基于 Tushare 取数 + 自研 `calculateMACD` 模块），本产品只需把"人工daily重复劳动"固化为一条自动流水线，不做策略创新。

**一句话定位**：一个工作日定时自动跑、基于 MACD 多周期信号、把"当天该买哪些指数"推送到个人微信（Server酱）的工具。

---

## 2. 目标与成功指标

### 2.1 产品目标
- 将每日研判的人工耗时从 ~15 分钟降到 0（完全自动化）。
- 保证信号口径与用户现有手工口径**一致**（以 `calculateMACD.py` 为参考基准）。
- 推送内容清晰、可复盘（每次推送即一份当日信号快照）。

### 2.2 成功指标（可衡量）
| 指标 | 基线 | 目标 |
|------|------|------|
| 每日推送成功率 | — | ≥ 99%（工作日） |
| 推送延迟（收盘→到达群） | 人工数小时 | ≤ 60 分钟（18:00–19:00 内到达） |
| 信号口径一致性 | 人工 | 与 `calculateMACD.py` 输出 100% 一致 |
| 误推/漏推 | — | 0（取数失败则降级告警，不静默发送错误信号） |

### 2.3 非目标（Non-goals）
- **不做策略研究**：不引入估值温度、动量、基本面等用户未确认的新因子。
- **不做回测/历史复盘系统**：本期只产出"当日信号"，不做信号历史库与胜率统计。
- **不做自动交易**：仅推送信号，交易动作由人执行（合规与风险考量）。
- **不做多渠道路由**：本期只推 Server酱（个人微信），不做邮件/飞书/短信。

---

## 3. 用户与用户故事

**核心用户（唯一）**：个人投资者 / 小团队负责人，有固定指数观察清单，依赖 MACD 做买卖决策。

用户故事：
- 作为投资者，我希望每天收盘后自动收到"哪些指数今天出现了买入信号"的清单，以便我不用自己逐个翻看。
- 作为投资者，我希望推送里同时看到日/2日/周三个周期的 MACD 数值，以便我判断信号的强弱与共振情况。
- 作为持仓者，我希望每天也能收到"哪些指数今天出现了卖出信号"，以便我及时减仓或离场，而不只是知道什么时候买。
- 作为投资者，我希望取数口径和我现有的 Tushare 脚本完全一致，以便我信任这套自动化结果。
- 作为投资者，我希望把信号推到自己（及家人/同伴）的个人微信，以便随时查看同一份研判。

---

## 4. 监控标的与接口映射

> 已与用户确认的监控标的共 **7 个**（含港股创新药 `931787.CSI` 经中证官网源、黄金 `AU9999` 经上海金交所现货源取数）。

| 标的 | 代码 | 取数接口 | 备注 |
|------|------|----------|------|
| 沪深300 | `000300.SH` | `index_daily` | A股宽基 |
| 科创50 | `000688.SH` | `index_daily` | A股宽基 |
| 创业板50 | `399673.SZ` | `index_daily` | 注意：非 `399006` 创业板指 |
| 恒生科技 | `HKTECH` | `index_global` | 港股指数，走新浪源 |
| 十年期国债 | `511260.SH` | `fund_daily` | 国泰上证10年期国债ETF（价格序列做MACD） |
| 港股创新药 | `931787.CSI` | `index_csi` | 中证香港创新药指数，经 akshare `stock_zh_index_hist_csindex`（中证官网源）取数 |
| 黄金 | `AU9999` | `spot_sge` | 上海金交所黄金现货，经 akshare `spot_hist_sge`（symbol=Au99.99，官网源）取数 |

**关键约束提示**：
- 数据源默认走 **akshare 免 token**（新浪/腾讯/中证/金交所官网源），无需 Tushare 积分；规避东方财富行情接口在部分网络下被断连。
- 创业板50 务必用 `399673`，与用户原 `创业板.py`（`399006`）区分开，避免口径错配。
- 标的清单**外置为 `indices.json`**，动态配置；新增/删除标的只改该文件、不改代码（详见 TRD §4.1）。清单里**只需填名称与代码**（`name` + `ts_code`），取数接口由系统按代码自动推断，用户无需关心（上表"取数接口"列仅为说明）。

---

## 5. 功能需求

### FR1 数据采集（akshare 免 token）
- 每个交易日收盘后，按上表逐标的调用对应 akshare 接口，取 `[date, close]` 序列。
- 免 token；如需切换为 Tushare（`DATA_SOURCE=tushare`），Token 通过环境变量 `TUSHARE_TOKEN` 注入，不硬编码。
- 失败处理：单标的取数失败 → 该标的标记"数据缺失"，不阻塞其余标的。

### FR2 MACD 多周期计算
- **算法口径**：以用户 `calculateMACD.py` 的 12/26/9 EWM 为参考基准，输出 `dif / dea / bar`；实现可采用更优写法，但须与参考基准逐值一致。
- **三个周期**：
  - 日线：原始日频 close 直接计算。
  - 2 日线：在日线基础上按"隔行取样"构造（与用户原脚本 `drop(df.index[1::2])` 口径一致）。
  - 周线：对日频 close 做周重采样（如 `W-FRI`）后计算。
- 输出：每个标的、每个周期的最新一行 dif/dea/bar 值。

### FR3 信号判定（买入 / 卖出，核心规则）
- **判定依据周期（basis，可按标的配置）**：每个标的在 `indices.json` 通过 `basis` 指定用哪个周期的 MACD 做判定——`daily`（日）/ `2d`（2日）/ `weekly`（周），默认 `daily`。例如十年期国债ETF 用 `weekly`（周线更适合债券这类低波动品种），其余标的用 `daily`。
- **买入触发**：判定周期的 **MACD 柱（BAR = DIF − DEA）较上一周期变多（增大）** → "当日买入信号"（动能增强）。
- **卖出触发**：判定周期的 **MACD 柱（BAR）较上一周期变少（减小）** → "当日卖出信号"（动能减弱）。
- **说明**：信号依据为 MACD **柱状体（BAR）的逐周期增减**，而非 DIF 线本身；DIF/DEA 仍作为展示字段输出。
- **对称性说明**：单个标的在单周期只可能处于"BAR 增大 / 减小 / 持平"三态之一，因此每个标的归入**买入块**或**卖出块**（BAR 持平时归入"未触发"），不会同时出现在两块中，避免信号自相矛盾。
- 输出字段：触发方向（买 / 卖 / 未触发）、各周期（日/2日/周）的 MACD 柱值、趋势箭头（↑柱变多 / ↓柱变少）及前一周期点值（括号展示，如 `0.88↑（0.66）`）。表格中**判定依据周期那一列会加粗**，其余周期仅展示、便于看共振。
- 不在此规则外做额外过滤（Non-goals 已声明）。

### FR4 推送（Server酱 → 个人微信）
- 通道：**Server酱³**（`POST https://sctapi.ftqq.com/<SENDKEY>.send`，表单参数 title + desp，desp 支持 markdown），推送到个人微信，免确认、实时自动送达。
- 凭证通过环境变量 `SERVERCHAN_SENDKEY` 注入；支持逗号分隔多个 SendKey，向多个收件人（各自的个人微信）分别推送，单个失败不影响其余。
- 频率约束：Server酱免费版有每日推送条数额度（每 SendKey 独立计），本期每工作日 3 条，通常在额度内。
- 失败处理：`code` 非 0 或网络异常 → 记录日志、失败重试 1 次；多收件人时单个失败不阻断其余，全部失败才抛错。

### FR5 调度
- 仅工作日运行，每天 **3 次**触发：**10:00 / 11:45 / 14:30**（盘中时段，便于依据信号决策；具体时序约束见下方说明）。
- 标题按触发时段自动切换：11 点前"上午报"、11~13 点"中午报"、13 点后"下午报"（北京时间）。
- 时序说明：盘中触发时取到的是**查询时刻的实时/最新可得数据**（盘中快照口径），非收盘后确定值；同一天不同时段推送的信号可能随行情变化而不同。若需"收盘后确定性信号"，应改到 15:00 收盘之后。
- 部署形态（详见 §8）：云函数定时触发（3 个定时触发器）。

---

## 6. 推送内容设计（Markdown 模板草案）

> 推送分 **两块**：买入信号与卖出信号，结论前置；判定周期 MACD 柱持平的标的归"未触发"。买入 / 卖出两块以**表格**呈现，每行一个标的，列出价格（最新收盘价）与日/2日/周 MACD 柱值、趋势箭头及前一周期值，便于横向对比。标题按触发时段动态显示"上午报/中午报/下午报"（下例以下午报为例）。**当没有未触发/数据缺失的标的时，不展示该分块。**

```markdown
# 📊 指数MACD信号下午报 (2026-08-22)

> 判定规则：根据日/2日/周MACD识别买入或卖出

## 🟢 今日买入信号

| 标的 | 价格 | 日 | 2日 | 周 |
|---|---|---|---|---|
| 沪深300 | 4618.90 | **0.88↑（0.66）** | 0.65↑（0.60） | 1.20↓（1.35） |

## 🔴 今日卖出信号

| 标的 | 价格 | 日 | 2日 | 周 |
|---|---|---|---|---|
| 科创50 | 1653.55 | **-0.36↓（-0.30）** | -0.30↓（-0.25） | -0.50↓（-0.40） |
| 十年期国债ETF | 135.76 | 0.01↓（0.02） | 0.06↓（0.08） | **0.21↓（0.21）** |

## — 未触发 / 数据缺失
- 恒生科技 (HKTECH)：数据缺失（取数失败，不参与判定）

## 📌 备注
- 表格中**加粗**的那一列是该标的的**判定依据**周期（如国债ETF按周MACD，其余按日MACD）
- 判定依据周期的 MACD柱较上一周期变多→买入、变少→卖出、持平→未触发；其余周期仅展示
- 本信号仅供参考，不构成投资建议
```

设计原则：先列"买入信号"（机会）与"卖出信号"（风险）两块结论，再列"未触发"（扫描完整性，仅在存在此类标的时展示），最后免责声明。买入 / 卖出两块采用表格，多标的时对齐清晰、便于横向对比；未触发 / 数据缺失仍用列表（条目少、以说明为主）。MACD 单元格省去前值标签、仅括号内保留前值（如 `0.88↑（0.66）`）缩短宽度，使 MACD 各列更窄、"标的"列相对更宽。

> ⚠️ 兼容性说明：Server酱个人微信端**不支持 `<br>` 换行、`<font>` 颜色等 HTML 标签**（会原样显示），故单元格内不换行、判定依据列用 **markdown 加粗**（而非标红）高亮。表格本身依赖 markdown 表格渲染，Server酱 `desp` 一般可正常显示；个别客户端降级为纯文本时可能对齐失效。

---

## 7. 系统数据流（逻辑架构）

```
┌─────────────┐   ┌──────────────┐   ┌──────────────┐   ┌──────────────┐
│ akshare      │ → │ 采集层        │ → │ MACD计算层    │ → │ 信号判定层    │
│ (新浪/腾讯源)│   │ (按标的取close)│  │ (日/2日/周)  │   │ (BAR↑触发)  │
└─────────────┘   └──────────────┘   └──────────────┘   └──────┬───────┘
                                                                │
                                                       ┌────────▼───────┐
                                                       │ 渲染 + 推送层   │
                                                       │ (Server酱markdown)│
                                                       └────────┬───────┘
                                                                │
                                                       ┌────────▼───────┐
                                                       │ 个人微信(Server酱)│
                                                       └────────────────┘
```
- 各层解耦，便于单独测试（如先验证 MACD 与手工一致，再接推送）。
- 环境变量驱动配置（SendKey / 数据源 / 标的清单可外置）。
- 数据源默认 akshare（免 token，新浪/腾讯源）；亦支持 Tushare（需 token）与 DEMO 合成数据。
- 复权口径：A股指数与 ETF 取**前复权**价（ETF 有分红，前复权才能与同花顺/新浪 App 的 MACD 一致；不复权会使 MACD 偏大）。

---

## 8. 部署与调度方案

| 维度 | 方案 A：本机 cron（轻量） | 方案 B：云函数（稳健） |
|------|--------------------------|------------------------|
| 适用 | 个人、单机常开 | 怕关机漏跑、需高可用 |
| 实现 | `30 14 * * 1-5 python daily_job.py` | 定时触发器 + 函数运行 |
| 优点 | 零成本、调试简单 | 不依赖本机在线 |
| 风险 | 关机即漏推 | 需云账号与少量费用 |

**部署选型（已定）**：最终采用 **方案 B 云函数**（定时触发器 + 函数运行，无需常驻机器）；验证阶段可先用方案 A 本机 cron 跑通信号口径，再迁移上云。

---

## 9. 风险与缓解

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| akshare 源接口变动/限流 | 对应标的取数为空 | 单标的失败标记缺失、不阻塞其余；均走新浪/腾讯/中证/金交所官网源，避开东财断连 |
| 非交易日误跑 / 假期数据缺失 | 推送噪声或报错 | 内置交易日历判断，非交易日直接跳过 |
| 推送被 Server酱限流/接口失效 | 收不到 | 推送失败告警 + 日级成功率监控 |
| 中证指数源（港股创新药）接口变动/限流 | 该标的取数失败 | 单标的失败标记缺失、不阻塞其余；`stock_zh_index_hist_csindex` 为中证官网源，相对稳定 |
| 信号误读导致交易亏损 | 用户风险 | 推送固定附"仅供参考，不构成投资建议"免责声明；本产品不做自动交易 |

---

## 10. 路线图（里程碑）

- **M1 方案确认**（本阶段）：标的、规则、接口、推送形态对齐 ✅
- **M2 取数验证**：akshare 各接口探针跑通，确认代码与取数口径无误
- **M3 计算对齐**：MACD 多周期输出与用户现有手工结果逐标的一致
- **M4 推送联调**：Server酱推送打通（真实发一条到个人微信），markdown 模板定稿
- **M5 上线调度**：云函数部署（工作日 3 个定时触发器 10:00/11:45/14:30），连续观察 1–2 周

---

## 11. 验收标准（Definition of Done）

1. 工作日 **10:00 / 11:45 / 14:30** 各触发一次，个人微信（Server酱）收到当日信号汇总（含全部 7 个标的状态），标题按时段显示上午/中午/下午报。
2. 任一标的 MACD 与用户 `calculateMACD.py` 手工结果逐值一致。
3. 取数失败时有明确"数据缺失"标记与告警，且不影响其余标的推送。
4. 非交易日自动跳过，不产生噪声消息。
5. 密钥（Server酱 SendKey，如启用 Tushare 则含 Token）仅来自环境变量，仓库/脚本中无明文密钥。

---

## 附：决策记录与待办（截至 2026-08-22）

**已确认（M2 前不再阻塞）：**
1. ✅ **创业板50 代码** = `399673`。
2. ✅ **十年期国债标的** = `511260.SH` 国债 ETF。
3. ✅ **推送时间**：工作日 3 次（10:00 / 11:45 / 14:30），采用盘中快照口径——即触发时查询所得的**实时/最新可得数据**，非收盘后确定值。（原方案为单次 14:30，后调整为 3 次）
4. ✅ **标的外置为 `indices.json`**（动态配置）：监控标的清单从代码常量改为 JSON 文件，`config.py` 读取校验；增删标的只改文件不改代码。
5. ✅ **港股创新药已接入**：`931787.CSI` 中证香港创新药指数，经 akshare `stock_zh_index_hist_csindex`（中证官网源，不走东财）取数，`api=index_csi`。监控标的由 5 个增至 6 个。
6. ✅ **2日/周线 MACD 参与判定**：已支持按标的配置判定周期（`basis`：daily/2d/weekly，默认 daily；如国债ETF 用 weekly），推送表格中对判定所依据的那一列加粗高亮；其余周期仍并列展示。
7. ✅ **黄金已接入**：`AU9999` 上海金交所黄金现货，经 akshare `spot_hist_sge`（symbol=Au99.99，官网源）取数，`api=spot_sge`。监控标的由 6 个增至 7 个。

**TODO（后续迭代，本期不做）：**
- [ ] **多周期共振判定**：当前每标的仅按单个 `basis` 周期判定，日/2日/周三周期尚未联合。后续可加入"日+2日+周同向共振"作为更强信号标识或过滤条件。

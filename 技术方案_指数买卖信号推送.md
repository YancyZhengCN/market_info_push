# 技术方案：指数 MACD 买卖信号自动推送

> 文档类型：技术方案 / 技术设计文档（TRD）  
> 对应 PRD：`产品方案_指数买卖信号推送.md`（v0.1）  
> 版本：v0.1  
> 创建日期：2026-08-22  
> 状态：待评审

---

## 1. 文档目的与范围

本文档将 PRD 中的功能需求翻译为**可实现的技术规格**，作为后续编码（M2–M5）的唯一依据。PRD 已锁定的决策本文档直接继承，不重新讨论。

**继承的已定决策（来自 PRD）：**

- 监控标的 7 个：沪深300(`000300.SH`)、科创50(`000688.SH`)、创业板50(`399673.SZ`)、恒生科技(`HKTECH`)、十年期国债(`511260.SH`)、港股创新药(`931787.CSI`，中证香港创新药指数)、黄金(`AU9999`，上海金交所现货)。
- 买入信号 = 日线 **MACD 柱（BAR = DIF − DEA）较前一日变多（增大）**；卖出信号 = 日线 **MACD 柱（BAR）较前一日变少（减小）**。BAR 持平归"未触发"。（信号依据为 MACD 柱增减，非 DIF 线）
- MACD 算法以用户现有 `calculateMACD.py` 为**参考基准**（12/26/9 EWM）；允许在口径一致前提下采用更优实现替换，但替换后输出须与参考实现逐值一致。
- 推送通道：**Server酱³**（`sctapi.ftqq.com`）推送到个人微信，markdown 文字，免确认实时送达；支持多 SendKey 多收件人。
- 数据源：默认 **akshare**（免 token，新浪/腾讯源），可选 Tushare（需 token）与 DEMO 合成数据。
- 调度：仅工作日，每天 3 次 **10:00 / 11:45 / 14:30**（标题按时段显示上午/中午/下午报）。
- 密钥（Server酱 SendKey，如启用 Tushare 则含 Token）仅来自环境变量。

**不在本文档范围**：策略研究、回测、自动交易、多渠道路由（见 PRD §2.3 Non-goals）。

---

## 2. 技术架构与模块划分

### 2.1 逻辑分层

```
┌─────────────────────────────────────────────────────────────┐
│                         main.py (编排)                        │
│  交易日判断 → 逐标的采集 → MACD计算 → 信号判定 → 渲染 → 推送   │
└─────────────────────────────────────────────────────────────┘
        │            │              │             │            │
   ┌────▼───┐  ┌─────▼─────┐  ┌─────▼─────┐  ┌────▼─────┐  ┌────▼─────┐
   │config  │  │ tushare_  │  │  macd /   │  │ signal   │  │ notifier │
   │.py     │  │ client.py │  │ calculate │  │ .py      │  │ .py      │
   │        │  │ (取数)    │  │ _macd.py  │  │          │  │(Server酱)│
   └────────┘  └───────────┘  └───────────┘  └──────────┘  └──────────┘
```

### 2.2 模块职责

| 模块                  | 职责                                            | 输入                | 输出                                            |
| ------------------- | --------------------------------------------- | ----------------- | --------------------------------------------- |
| `config.py`         | 加载环境变量与标的清单，校验必填项                             | `.env` / 系统环境     | `Config` 对象（sendkey、data_source、indices[]）    |
| `tushare_client.py` | 取数（默认 akshare 新浪/腾讯源，可选 tushare/DEMO），统一返回 close 序列 | IndexConfig, Config | `Series[close]`（DatetimeIndex 升序）           |
| `macd.py`           | 参考 `calculateMACD.py` 口径实现 MACD，对日/2日/周序列计算 dif/dea/bar | close 序列          | 各周期最新一行 MACD 值                                |
| `signals.py`        | 按 MACD 柱(BAR) 逐日增减判定买/卖/未触发               | 各标的日线 MACD        | `Signal(BUY/SELL/HOLD/MISSING, 多周期 PeriodMACD, price)` |
| `notifier.py`       | 渲染 markdown 并通过 Server酱推送到个人微信（支持多 SendKey） | `Signal[]` + 日期   | 推送结果摘要 / 异常                                  |
| `main.py`           | 编排全流程（多标的**并发**取数+计算），处理跳过/失败/告警        | 命令行参数             | 退出码 + 日志                                      |

### 2.3 建议目录结构

```
market_info_to_wechat/
└── src/                  # 实际代码目录
    ├── main.py           # 编排入口（云函数 main_handler）
    ├── config.py         # 环境变量 + 读取 indices.json
    ├── tushare_client.py # 取数层（index_daily/index_global/fund_daily，懒加载 tushare）
    ├── macd.py           # MACD 计算（对齐 calculateMACD.py）
    ├── signals.py        # 信号判定（避开标准库 signal 命名）
    ├── templates.py      # markdown 渲染
    ├── notifier.py       # Server酱推送（个人微信，requests 懒加载）
    ├── calculateMACD.py  # 参考基准（M3 对齐测试用，不强制内联复用）
    ├── indices.json      # 监控标的清单（动态配置）
    ├── requirements.txt
    ├── .env.example
    └── tests/
        ├── test_macd.py
        ├── test_signals.py
        └── test_render.py
```

---

## 3. 运行环境与依赖

| 项    | 要求                                                    |
| ---- | ----------------------------------------------------- |
| 语言   | Python 3.9+（与用户现有 TuShare 脚本环境一致）                     |
| 核心依赖 | `akshare`、`pandas`、`numpy`、`requests`、`python-dotenv`、`urllib3<2`（可选 `tushare`） |
| 取数前置 | 默认 akshare 免 token；如启用 Tushare 则需账户积分（`index_global` 门槛较高）   |
| 网络   | 可访问 akshare 数据源（新浪/腾讯行情）与 Server酱域名（`sctapi.ftqq.com`）  |
| 部署   | 云函数定时触发（见 §9）                          |

`requirements.txt` 草案：

```
tushare>=1.2.89
pandas>=1.5
numpy>=1.23
requests>=2.28
python-dotenv>=1.0
akshare>=1.14
urllib3<2
```

---

## 4. 配置项（环境变量）

| 变量                  | 必填 | 说明                                  | 示例                                                          |
| ------------------- | -- | ----------------------------------- | ----------------------------------------------------------- |
| `SERVERCHAN_SENDKEY`| 是  | Server酱 SendKey；逗号分隔可多收件人           | `SCTxxx` 或 `SCTa,SCTb`                                      |
| `DATA_SOURCE`       | 否  | 数据源：`akshare`(默认,免token) / `tushare` | `akshare`                                                   |
| `TUSHARE_TOKEN`     | 否  | 仅 `DATA_SOURCE=tushare` 时必填         | `your_token_here`                                           |
| `PUSH_ENABLED`      | 否  | `true` 才真实推送；`false` 仅本地打印（dry-run） | `true`                                                      |
| `FORCE_RUN`         | 否  | `true` 跳过交易日判断（调试用；生产设 `false`）     | `false`                                                     |
| `INDICES_URL`       | 否  | 远程标的清单 URL（设了则优先，否则读本地 `indices.json`） | `https://xxx/indices.json`                             |
| `LOG_LEVEL`         | 否  | 日志级别                                | `INFO`                                                      |

> 监控标的（代码/接口/名称）**外置为 `indices.json`**，由 `config.py` 读取并校验，不在环境变量、也不写死在代码中；新增/删除标的只改该 JSON 文件即可（云函数场景需重新部署以打包新文件，后续可升级为对象存储远程拉取，见 §4.1）。密钥类仍仅来自环境变量。

### 4.1 标的清单 `indices.json`（动态配置）

- **位置**：与代码同目录，随函数打包；`config.py` 启动时读取并校验（缺字段 / 未知 `api` 值时 ERROR 退出，不静默）。
- **Schema**：JSON 数组，每项一个标的，通常只需 `name` + `ts_code`：
  ```json
  [
    { "name": "沪深300",      "ts_code": "000300.SH" },
    { "name": "恒生科技",     "ts_code": "HKTECH"    },
    { "name": "十年期国债ETF", "ts_code": "511260.SH" },
    { "name": "港股创新药",    "ts_code": "931787.CSI" }
  ]
  ```
- **字段**：`name` 展示名；`ts_code` 标的代码；`api` **可选**（取数接口类型），留空则由 `config.py` 按 `ts_code` **自动推断**，用户无需感知接口细节：
  - 以 `AU` 开头（如 `AU9999`）→ `spot_sge`（上海金交所黄金现货，如黄金）
  - `.SH` / `.SZ` 结尾 → `index_daily`（A股指数与 ETF 同走腾讯前复权）
  - `.CSI` 结尾 → `index_csi`（中证指数，如港股创新药）
  - 其余非数字代码（如 `HKTECH`）→ `index_global`（港股指数，新浪源）
  - 需要覆盖推断时可显式写 `api`（合法值 `index_daily`/`index_global`/`fund_daily`/`index_csi`/`spot_sge`）。可选 `lookback`（默认 120）。
  - 可选 `basis`（判定依据周期）：`daily`（默认）/ `2d` / `weekly`，指定该标的用哪个周期的 MACD 做买卖判定（如国债ETF 用 `weekly`）。
- **增删标的**：只改此文件，不加代码（前提是 `ts_code` 能被上述规则推断或 `api` 属于已支持类型；新增取数类型才需加分支）。
- **升级路径（非本期）**：将本文件改为放 COS/OSS，函数运行时按 `INDICES_URL` 拉取，则改标的无需重新部署（本地文件作兜底）。

---

## 5. 接口规格

### 5.1 取数（默认 akshare，可选 Tushare / DEMO）

统一取"截至今日、向前约 120 个交易日"的日频收盘价，保证 MACD(26) 与 DEA(9) 充分预热（最少需 ≥35 根，取 120 留足余量）。所有源统一返回 DatetimeIndex 升序、去空的 `close` Series。

**默认源 akshare（免 token）**：A股指数与 ETF 走**腾讯源前复权**（对齐同花顺/新浪 App 的 MACD 口径），港股指数走**新浪指数接口**。规避东方财富行情接口在部分网络下被断连。

| 标的类型 | 接口                        | 关键参数              | 说明               |
| ---- | ------------------------- | ----------------- | ---------------- |
| A股指数 | `ak.stock_zh_a_hist_tx`   | `symbol=sh000300`, `adjust='qfq'` | 腾讯源，前复权（指数无分红，值同原始） |
| ETF  | `ak.stock_zh_a_hist_tx`   | `symbol=sh511260`, `adjust='qfq'` | 腾讯源，**前复权**（ETF 有分红，必须前复权才与同花顺一致） |
| 港股指数 | `ak.stock_hk_index_daily_sina` | `symbol=HSTECH` | 新浪港股指数（腾讯 A 股接口取不到港股，指数无复权） |
| 中证指数 | `ak.stock_zh_index_hist_csindex` | `symbol=931787` | 中证官网源（如港股创新药；不走东财，返回中文列名 日期/收盘，需映射） |
| 黄金现货 | `ak.spot_hist_sge` | `symbol=Au99.99` | 上海金交所官网源（如黄金 AU9999；无复权概念，返回 date/open/close/low/high） |

> ts_code → symbol 映射：`000300.SH`→`sh000300`、`399673.SZ`→`sz399673`、`511260.SH`→`sh511260`、`HKTECH`→`HSTECH`、`AU9999`→`Au99.99`。
> ⚠️ 复权口径：ETF 分红会使**不复权**价的历史序列偏高，导致 MACD 偏大（如国债ETF不复权 BAR≈0.05，前复权≈0.01）。同花顺/东财/新浪 App 均默认前复权，故本项目 ETF 采用**前复权**对齐。
> ⚠️ 不要用 akshare 的 `fund_etf_hist_sina`（仅不复权）/ `index_zh_a_hist` / `fund_etf_hist_em`（底层东财，会被断连）。
> ⚡ 取数范围（性能）：腾讯源 `stock_zh_a_hist_tx` 按 `start_date = today - (max(lookback,60)*2+40) 天` 计算起始日，**只拉最近约一年**（几百行），而非全历史（5000+ 行）。实测单标的从 ~10s 降至 ~1s。MACD 只看相对变化、预热 120+ 根即收敛，实测有界范围与全历史的 daily/2d/weekly 三周期 BAR **数值完全一致**，判定不受影响（含对复权最敏感的国债 ETF）。

**可选源 Tushare（需 token，`DATA_SOURCE=tushare`）**：

| 标的类型 | 接口                 | 关键参数                                         | 返回字段（取用）              |
| ---- | ------------------ | -------------------------------------------- | --------------------- |
| A股指数 | `pro.index_daily`  | `ts_code`, `start_date`, `end_date`          | `trade_date`, `close` |
| 港股指数 | `pro.index_global` | `ts_code='HKTECH'`, `start_date`, `end_date` | `trade_date`, `close` |
| ETF  | `pro.fund_daily`   | `ts_code`, `start_date`, `end_date`          | `trade_date`, `close` |

- **DEMO 源**：`DEMO=true` 时按 ts_code 哈希生成确定性合成序列，无需联网/ token，用于离线跑通全流程。
- **失败语义**：单标的返回空或抛异常 → 标记 `MISSING`，跳过该标的计算与判定，不阻塞其余标的。
- **字段对齐**：日期统一转为 `datetime`，按日期升序排列后再算 MACD。

### 5.2 Server酱推送（markdown → 个人微信）

> 推送走 **Server酱³**（`sctapi.ftqq.com`），把日报推送到个人微信，免确认、实时自动送达；`desp` 字段支持 markdown。

- **接口**：`POST https://sctapi.ftqq.com/<SENDKEY>.send`，表单参数 `title`（标题，取正文首行）+ `desp`（正文，完整 markdown）。
- **多收件人**：`SERVERCHAN_SENDKEY` 支持逗号分隔多个 SendKey，逐个发送；每个收件人各自登录 https://sct.ftqq.com/ 获取自己的 key。
- **Body 示例**（application/x-www-form-urlencoded）：

```
title=📊 指数MACD信号下午报 (2026-08-22)
desp=<见 §8 渲染规格的完整 markdown>
```

- **返回**：JSON `{"code": 0, ...}`，`code == 0` 为成功。
- **限流/频控**：Server酱免费版有每日推送条数额度（每 SendKey 独立计）；本期每工作日 3 条，通常在额度内。
- **失败处理**：`code` 非 0 或网络异常 → 记录日志、失败重试 1 次（间隔 5s）；多收件人时单个失败仅记日志、不阻断其余，全部失败才抛错。
- **兼容性说明**：Server酱个人微信端**不支持 `<br>` 等 HTML 换行标签**（会原样显示为文本）；markdown 表格一般可正常渲染，个别客户端降级为纯文本时可能对齐失效。

---

## 6. MACD 计算规格（精确）

> **参考基准**：以用户现有 `calculateMACD.py` 的口径为基准（12/26/9 EWM、BAR 定义、日/2日/周构造方式）；允许采用更优写法替换实现，但替换后输出须与参考实现**逐值一致**。以下为其约定口径，供替换实现对齐验证时逐值核对。

- **参数**：快线 12、慢线 26、信号线 9，全部采用 EWM（`adjust=False`，即 `pandas.Series.ewm(span=N, adjust=False).mean()`）。
- **公式**：
  ```
  EMA12 = ewm(close, 12)
  EMA26 = ewm(close, 26)
  DIF   = EMA12 - EMA26
  DEA   = ewm(DIF, 9)
  BAR   = (DIF - DEA)        # 具体系数以 calculateMACD.py 实际实现为准（常见为 1× 或 2×）
  ```
- **三个周期构造**（必须按用户原口径，否则信号不一致）：
  - **日线**：原始日频 `close` 直接算。
  - **2 日线**：日频序列按"隔行取样"构造 —— 保留索引为 0,2,4,… 的行（即 `df.iloc[::2]` / 等价于原脚本 `drop(df.index[1::2])`），再算 MACD。
  - **周线**：日频 `close` 重采样为周线（`resample('W-FRI').last()` 取每周最后收盘价），再算 MACD。
- **输出**：每个标的、每个周期返回**最新一行 + 上一周期点**（`{dif, dea, bar, bar_prev}`）；`bar_prev` 用于展示趋势箭头（↑ 当前>上一周期点 / ↓ 当前<上一周期点）。信号判定用**该标的 `basis` 指定周期**的 BAR 增减（bar vs bar_prev），默认 `daily`。

---

## 7. 信号判定逻辑

### 7.1 规则

- 取**该标的 `basis` 指定周期**（daily/2d/weekly，默认 daily）MACD 最新两行：`bar_t`（本周期 MACD 柱）、`bar_{t-1}`（上一周期 MACD 柱）。
- `bar_t > bar_{t-1}` → **买入（BUY）**（柱变大，动能增强）
- `bar_t < bar_{t-1}` → **卖出（SELL）**（柱变小，动能减弱）
- `bar_t == bar_{t-1}` → **未触发（HOLD）**
- 单标的单周期只可能落入三者之一，互不矛盾（见 PRD FR3 对称性说明）。

### 7.2 伪代码

```python
def judge(m):  # m 为判定周期的 MACDResult
    if m.bar is None:       return "MISSING"
    if m.bar_prev is None:  return "HOLD"
    if m.bar > m.bar_prev:  return "BUY"
    if m.bar < m.bar_prev:  return "SELL"
    return "HOLD"

for idx in indices:
    if idx.data_missing:
        results.append(missing_signal(idx.name, idx.ts_code, idx.basis))
        continue
    d_daily  = macd.compute(idx.close_daily, period="daily")
    d_p2d    = macd.compute(idx.close_daily, period="2d")
    d_weekly = macd.compute(idx.close_daily, period="weekly")
    basis_result = {"daily": d_daily, "2d": d_p2d, "weekly": d_weekly}[idx.basis]
    status = judge(basis_result)     # 用 basis 指定周期的 BAR 增减
    results.append(Signal(
        name=idx.name, ts_code=idx.ts_code, status=status,
        daily=PeriodMACD(d_daily.bar, d_daily.bar_prev),
        p2d=PeriodMACD(d_p2d.bar, d_p2d.bar_prev),
        weekly=PeriodMACD(d_weekly.bar, d_weekly.bar_prev),
        price=idx.last_close, basis=idx.basis,
    ))
```

> ⚡ 并发编排：上述"逐标的处理"（取数→三周期 MACD→判定）已封装为 `_process_index()`，在 `main.py` 中通过 `ThreadPoolExecutor(max_workers=min(8, 标的数)).map(...)` **并发执行**。取数以网络 IO 为主，GIL 不阻塞 IO 等待，多标的近似取"最慢标的"耗时而非累加；`map` 保序，输出顺序与 `indices.json` 一致；单标的异常在 `_process_index` 内被捕获并降级为 `MISSING`，不阻塞其余。

### 7.3 输出数据结构（建议）

```python
@dataclass
class PeriodMACD:
    bar: float | None          # 当前 MACD 柱
    bar_prev: float | None     # 上一周期点 MACD 柱（日线=昨日，2日线=上一2日点，周线=上周）
    @property
    def trend(self) -> str:    # "↑" 柱变多 / "↓" 柱变少 / "—" 持平或缺失
        if self.bar is None or self.bar_prev is None:
            return "—"
        if self.bar > self.bar_prev:  return "↑"
        if self.bar < self.bar_prev:  return "↓"
        return "—"

@dataclass
class Signal:
    name: str
    ts_code: str
    status: str          # BUY | SELL | HOLD | MISSING
    daily: PeriodMACD    # 日线 MACD
    p2d: PeriodMACD      # 2日线 MACD
    weekly: PeriodMACD   # 周线 MACD
    price: float | None = None   # 最新收盘价
    basis: str = "daily"         # 判定依据周期（daily/2d/weekly）；渲染时该列加粗
```

---

## 8. 推送消息渲染规格

### 8.1 结构与字段

- 标题：`# 📊 指数MACD信号{时段}报 (YYYY-MM-DD)`，`{时段}` 按北京时间触发时刻取：<11 点"上午"、11~13 点"中午"、>=13 点"下午"
- 规则说明行：根据日/2日/周MACD识别买入或卖出
- 块1 🟢 今日买入信号：**markdown 表格**，表头 `| 标的 | 价格 | 日 | 2日 | 周 |`，每行一个标的（价格为最新收盘价），MACD 单元格为 `{bar}↑/↓（{前周期值}）`（**全角括号**：CJK 字符提供换行断点，单元格可折行更窄、避免整表超出手机屏宽；省去前值标签、仅括号内保留前值以缩短单元格宽度；箭头由该周期当前 BAR vs 上一周期点得出）。**该标的 `basis` 指定的判定周期那一列用 markdown 加粗高亮**。无标的时输出 `- 无`
- 块2 🔴 今日卖出信号：同表格结构（判定列同样加粗）。无标的时输出 `- 无`
- 块3 — 未触发 / 数据缺失：**列表**（条目少、以说明为主），列出 HOLD 与 MISSING 标的，MISSING 标注"数据缺失"。**仅在存在此类标的时才输出该分块（`other` 为空则整块省略）**
- 备注：说明"加粗列=判定依据周期"；判定周期 BAR 持平归未触发；固定免责声明"本信号仅供参考，不构成投资建议"

> 样式说明：买入/卖出两块用表格，多标的时对齐清晰、便于横向对比（表格不含"代码"列，代码仅在未触发/数据缺失列表中随标的名展示）；MACD 单元格省去前值标签、仅括号内保留前值缩短列宽；判定依据列用 markdown 加粗（`**...**`）高亮。**Server酱个人微信端不支持 `<br>` 换行、`<font>` 颜色等 HTML 标签**（会原样显示），故不换行、不标红。表格本身依赖 markdown 渲染，Server酱 `desp` 支持；个别客户端降级为纯文本时可能对齐失效。

### 8.2 渲染伪代码

```python
TABLE_HEADER = ["| 标的 | 价格 | 日 | 2日 | 周 |", "|:---:|:---:|:---:|:---:|:---:|"]  # 列全部居中
CST = timezone(timedelta(hours=8))  # 固定东八区，避免云函数默认 UTC 导致时段误判

def session_label(hour):  # <11 上午 | 11~13 中午 | >=13 下午
    return "上午" if hour < 11 else ("中午" if hour < 13 else "下午")

def render(results, date, hour=None):
    buy   = [r for r in results if r.status == "BUY"]
    sell  = [r for r in results if r.status == "SELL"]
    other = [r for r in results if r.status in ("HOLD", "MISSING")]
    hour = datetime.now(CST).hour if hour is None else hour  # 按北京时间取时段
    lines = [f"# 📊 指数MACD信号{session_label(hour)}报 ({date})",
             "> 判定规则：根据日/2日/周MACD识别买入或卖出", ""]
    lines += ["## 🟢 今日买入信号", ""]
    lines += table(buy) if buy else ["- 无"]
    lines += ["", "## 🔴 今日卖出信号", ""]
    lines += table(sell) if sell else ["- 无"]
    if other:  # 仅在存在未触发/数据缺失标的时才展示该分块
        lines += ["", "## — 未触发 / 数据缺失"]
        lines += [fmt_other(r) for r in other]
    lines += ["", "## 📌 备注", "- 表格中**加粗**的那一列是该标的的**判定依据**周期（日/2日/周 MACD）",
              "- 判定依据周期的 MACD柱较上一周期变多→买入、变少→卖出、持平→未触发；其余周期仅展示",
              "- 本信号仅供参考，不构成投资建议"]
    return "\n".join(lines)

BASIS_ATTR = {"daily": "daily", "2d": "p2d", "weekly": "weekly"}

def table(rows):
    out = list(TABLE_HEADER)
    for r in rows:
        c = {"daily": cell(r.daily), "p2d": cell(r.p2d), "weekly": cell(r.weekly)}
        k = BASIS_ATTR[r.basis]
        c[k] = f"**{c[k]}**"   # 判定依据周期列加粗高亮
        out.append(f"| {r.name} | {price(r.price)} | {c['daily']} | {c['p2d']} | {c['weekly']} |")
    return out

def price(v):
    return f"{v:.2f}" if v is not None else "—"

def cell(m):
    # 表格单元格：当前 BAR+箭头（前一周期值），省去前值标签缩短列宽（Server酱不支持 <br> 换行）
    return f"{m.bar:.2f}{m.trend}（{m.bar_prev:.2f}）" if (m.bar is not None and m.bar_prev is not None) else "—"

def p(m):
    # 列表单元格（未触发/数据缺失块用）：单行，当前 BAR+箭头(前一周期值)
    return f"{m.bar:.2f}{m.trend}({m.prev_label}{m.bar_prev:.2f})" if (m.bar is not None and m.bar_prev is not None) else "—"

def fmt_other(r):
    if r.status == "MISSING":
        return f"- **{r.name}** ({r.ts_code})：数据缺失（取数失败，不参与判定）"
    return f"- **{r.name}** ({r.ts_code})：日MACD {p(r.daily)} ｜ 2日MACD {p(r.p2d)} ｜ 周MACD {p(r.weekly)}"
```

---

## 9. 调度规格（部署选型：方案 B 云函数）

- **触发**：仅工作日（周一至周五，且非节假日），每天 3 次 **10:00 / 11:45 / 14:30**。
- **部署选型（已定）**：采用 **云函数**（事件函数类型），由平台定时触发器拉起，无需常驻机器、不怕关机漏推。平台推荐腾讯云 SCF 或阿里云 FC。
- **落地细节**：
  - **入口**：`main.py` 暴露 `def main_handler(event, context):` 作为函数入口；`if __name__ == "__main__":` 分支仅本地调试用，不影响云端。函数类型选**事件函数**（非 Web 函数）。
  - **定时触发器**：因 3 个时间点分钟各异，需建 **3 个定时触发器**（SCF 7 段 cron）：`0 0 10 ? * MON-FRI *`、`0 45 11 ? * MON-FRI *`、`0 30 14 ? * MON-FRI *`；**务必确认平台时区为 UTC+8（Asia/Shanghai）**，否则错峰触发。
  - **环境变量**：注入 `SERVERCHAN_SENDKEY`、`DATA_SOURCE=akshare`、`PUSH_ENABLED=true`、`FORCE_RUN=false`（不写入代码/包）。
  - **依赖打包**：含 C 扩展的包（numpy/pandas/lxml/curl_cffi/mini-racer）需 **Linux 版 wheel**，用 `deploy/build_package_nodocker.sh` 在 macOS 上打出 Linux 兼容包；`urllib3` 必须锁 **<2**（云函数 OpenSSL 1.0.2k 不兼容 urllib3 v2）。
  - **超时与内存**：函数超时设为 **120s**（默认 3s 会因取数超时失败）；内存 128–256MB 足够。
  - **日志**：使用平台日志服务（如 SCF 运行日志 / CLS），代码保持结构化日志即可。
  - **费用**：每工作日 3 次、每次约 1 分钟运行，落在免费额度内，几乎零成本。
- **时序约束**：盘中触发取到的是**查询时刻的实时/最新可得数据**（盘中快照口径），非收盘后确定值；同一天不同时段信号可能不同。如需收盘后确定性信号应改为 15:00 之后。
- **交易日过滤**：运行前调用交易日历判断；非交易日直接 `exit(0)` 不推送。注：akshare 无 token 场景下退化为"仅判断是否工作日"，不识别法定节假日（`FORCE_RUN=true` 可跳过该判断，仅调试用）。

---

## 10. 错误处理与日志

| 场景            | 处理                                       |
| ------------- | ---------------------------------------- |
| 单标的取数失败/空     | 标记 `MISSING`，跳过计算与判定，其余继续                |
| 数据源限流/异常 | 捕获异常，该标的 `MISSING`，不重试阻塞整体               |
| 全部标的缺失        | 仍推送（含"全部数据缺失"提示）或仅告警，由 `PUSH_ENABLED` 控制 |
| Server酱推送失败    | 日志 ERROR + 失败重试 1 次；多收件人单个失败不阻断其余，全部失败才抛错 |
| 环境变量缺失        | 启动即校验，缺失则 ERROR 退出，不空跑                   |
| 非交易日          | `exit(0)`，无输出                            |

- **日志**：结构化输出到控制台（云函数日志服务采集），含每标的取数行数、MACD 柱(BAR) 值、判定结果、推送成功数。
- **幂等**：每次触发独立运行，无需去重；同日多次触发即多次推送（本期特性，每天 3 次）。

---

## 11. 测试策略

| 层级         | 内容                                      | 方法                                |
| ---------- | --------------------------------------- | --------------------------------- |
| 单元测试-算法    | 新/替换实现输出与参考 `calculateMACD.py` 逐值一致 | 用同一段 close 序列分别跑参考实现与新实现，断言相等（M3 对齐核心） |
| 单元测试-信号    | `judge()` 在合成 **MACD 柱(BAR)** 序列上返回正确 BUY/SELL/HOLD | 构造 BAR 上升/下降/持平三组输入断言                  |
| 单元测试-渲染    | `render()` 正确分块、缺失处理、免责声明存在             | 固定输入比对输出字符串                       |
| 集成测试-推送    | Server酱推送联通（真实发一条到个人微信） | 真实推送一条验证可达与 markdown 渲染              |
| 冒烟/dry-run | `PUSH_ENABLED=false` 打印 markdown 不发送    | 手动核对格式与分块                         |

> **M3 关键验收**：MACD 多周期输出与用户现有手工结果**逐标的一致**，是上线前置条件。

---

## 12. 非功能性需求

- **性能**：单次运行端到端约 **4–5s**（7 标的）。两项关键优化：① 腾讯源按 lookback 计算起始日、**只拉最近约一年**而非全历史（单标的 ~10s→~1s）；② 多标的**并发取数**（线程池），总耗时≈最慢标的而非累加。优化前串行+全历史约 27s。
- **可靠性**：单点失败不整机崩溃；单标的缺失不影响其余；推送失败可观测。
- **安全**：密钥仅环境变量，`.env` 必须加入 `.gitignore`，代码中零硬编码。
- **可观测**：每日运行留痕（日志 + 推送结果），便于复盘与故障定位。
- **可维护**：模块解耦，MACD 实现可独立替换（以 `calculateMACD.py` 口径为参考）；标的清单外置为 `indices.json`，增删已支持类型的标的仅改该 JSON 文件、不动代码（新增取数类型才需加分支）。
- **可扩展（非本期）**：信号历史库、共振加权、多通道，均为后续迭代，不在本期实现。

---

## 13. 上线检查清单（DoD 技术版）

- [x] `config.py` 校验 `SERVERCHAN_SENDKEY` 存在（`DATA_SOURCE=tushare` 时还需 `TUSHARE_TOKEN`）
- [x] 7 标的取数成功（akshare 新浪/腾讯/中证/金交所源）
- [x] MACD 替换实现输出与参考 `calculateMACD.py` 逐值一致（M3）
- [x] Server酱推送联通测试通过（markdown 可达个人微信，多 SendKey 均送达）
- [x] 工作日 3 个 cron（10:00/11:45/14:30）生效，非交易日自动跳过
- [x] 单标的缺失时其余正常推送，且 MISSING 正确标记
- [x] 推送 markdown 分块正确（买入/卖出表格含价格列、未触发/缺失列表），含免责声明
- [x] 日志完整、密钥无泄漏

---

## 14. 决策记录与待办（截至 2026-08-22）

**已确认：**
1. ✅ 创业板50 代码 = `399673`。
2. ✅ 十年期国债标的 = `511260.SH` ETF。
3. ✅ 14:30 盘中快照口径 = 查询时刻的实时/最新可得数据（已确认）。
4. ✅ 港股创新药 `931787.CSI` 已接入（`api=index_csi`，中证官网源），监控标的增至 6 个。
5. ✅ 判定依据周期可按标的配置（`basis`：daily/2d/weekly，默认 daily；国债ETF 用 weekly）；表格中判定列加粗高亮。
6. ✅ 黄金 `AU9999` 已接入（`api=spot_sge`，上海金交所现货 `ak.spot_hist_sge`，symbol=Au99.99），监控标的增至 7 个。

**TODO（后续迭代）：**
- [ ] 多周期共振判定（当前每标的仅按单个 `basis` 周期判定；后续可做"日+2日+周"同向共振作为更强信号）。
